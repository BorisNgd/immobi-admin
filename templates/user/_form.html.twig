{{ form_start(form) }}

<div class="card-body">

    <div class="row">
        <div class="col-md-12">
            <div class="card shadow mb-4">
                <div class="card-body">
                    <div class="card-header text-left bg-transparent">
                        <h6>USER UPDATE FORM</h6>
                    </div>

                    <div class="row form-group m-2">
                        <div class="col-md-2 justify-content-center">
                            {{ form_label(form.photoFile) }}
                        </div>
                        <div class="col-md-10">
                            {{ form_widget(form.photoFile , {'attr':{'class':''}}) }}
                            <small>{{ form_help(form.photoFile) }}</small>
                            <div class="form-error">
                                {{ form_errors(form.photoFile) }}
                            </div>
                        </div>
                    </div>

                    <div class="row form-group m-2">
                        <div class="col-md-2 justify-content-center">
                            {{ form_label(form.email) }}
                        </div>
                        <div class="col-md-10">
                            {{ form_widget(form.email , {'attr':{'class':''}}) }}
                            <small>{{ form_help(form.email) }}</small>
                            <div class="form-error">
                                {{ form_errors(form.email) }}
                            </div>
                        </div>
                    </div>



                    <div class="row form-group m-2">
                        <div class="col-md-2 justify-content-center">
                            {{ form_label(form.roles) }}
                        </div>
                        <div class="col-md-10">
                            {{ form_widget(form.roles , {'attr':{'class':''}}) }}
                            <small>{{ form_help(form.roles) }}</small>
                            <div class="form-error">
                                {{ form_errors(form.roles) }}
                            </div>
                        </div>
                    </div>

                    <div class="row form-group m-2">
                        <div class="col-md-2 justify-content-center">

                        </div>
                        <div class="col-md-10">
                            {{ form_widget(form.password , {'attr':{'class':''}}) }}
                            <small>{{ form_help(form.password) }}</small>
                            <div class="form-error">
                                {{ form_errors(form.password) }}
                            </div>
                        </div>
                    </div>


                    <div class="row">
                        <div class="col-md">
                            <div class="row">
                                <div class="col-md text-center">
                                    <button class="btn btn-primary btn-xs"><i class="fa fa-save"></i>{{ button_label|default('Save') }}</button>

                                </div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>

    </div>

</div>

{{ form_end(form) }}
